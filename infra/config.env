# Checked, non-secret runtime configuration. IMAGE is supplied by the operator.
PROJECT_ID=REPLACE_PROJECT_ID
REGION=us-central1
SERVICE_ACCOUNT=REPLACE_SERVICE_ACCOUNT
RUNTIME_ENV=REPLACE_RUNTIME_ENV
WORKER_POOL_SPECS=REPLACE_POOL_NAME:REPLACE_ROLE:1
