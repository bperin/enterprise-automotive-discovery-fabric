steps:
- name: gcr.io/cloud-builders/docker
  args: [build, -f, infra/Dockerfile, -t, us-central1-docker.pkg.dev/$PROJECT_ID/enterprise-search/worker:$COMMIT_SHA, .]
images:
- us-central1-docker.pkg.dev/$PROJECT_ID/enterprise-search/worker:$COMMIT_SHA
